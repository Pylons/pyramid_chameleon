[tox]
envlist = 
    py27,pypy,py34,py35,py36,py37,pypy3,
    docs,
    {py2,py3}-cover,coverage

[testenv]
commands = 
    pip install pyramid_chameleon[testing]
    nosetests --with-xunit --xunit-file=nosetests-{envname}.xml {posargs:}

[py-cover]
commands =
    pip install pyramid_chameleon[testing]
    coverage run --source=pyramid_chameleon --parallel-mode {envbindir}/nosetests
    coverage combine
    coverage xml -o {envname}.xml

[testenv:py2-cover]
commands =
    {[py-cover]commands}
setenv =
    COVERAGE_FILE=.coverage.py2

[testenv:py3-cover]
commands =
    {[py-cover]commands}
setenv =
    COVERAGE_FILE=.coverage.py3

[testenv:coverage]
basepython = python3.5
commands =
    coverage erase
    coverage combine
    coverage xml
    coverage report --show-missing --fail-under=100 --omit=pyramid_chameleon/tests/fixtureapps/getline.py
deps =
    coverage
setenv =
    COVERAGE_FILE=.coverage

# we separate coverage into its own testenv because a) "last run wins" wrt
# cobertura jenkins reporting and b) pypy and jython can't handle any
# combination of versions of coverage and nosexcover that i can find.

[testenv:docs]
basepython =
    python3.5
whitelist_externals = make
commands =
    pip install pyramid_chameleon[docs]
    make -C docs clean html epub BUILDDIR={envdir} "SPHINXOPTS=-W -E"
